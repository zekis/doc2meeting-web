"""Markdown parsing + TTS text-cleaning utilities.

Vendored from the standalone `doc2meeting` package — only `parser` and
`cleaner` were ever imported, so they live in-tree to keep the web app
self-contained.
"""
